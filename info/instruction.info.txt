--- JUST INFO

Isso não é erro — é só o Git dizendo:
“você nunca me disse para onde eu devo enviar esse main.”
✅ Faça uma única vez:



git push --set-upstream origin main

ou, abreviado:
bash
Copy

git push -u origin main

Depois disso, o Git lembra — da próxima vez é só:
bash
Copy


/// push to git

git add .
git commit -m "descrição"
git push
Sem mais mensagens.
